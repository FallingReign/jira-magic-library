# Test-specific environment variables
# Copy to .env.test and fill with safe test values (non-production).

# JIRA configuration
JIRA_BASE_URL=http://localhost:2990
JIRA_PAT=example-token
JIRA_API_VERSION=v2
JIRA_PROJECT_KEY=TEST
# JIRA_PROJECT_KEY_NO_PARENT=TESTNOPARENT # optional: any project that has no parent/Epic/Parent Link field configured - otherwise ignore

# Test users (used by converters/integration tests)
JIRA_TEST_USER_EMAIL=user@example.com
JIRA_TEST_USER_NAME=John Doe
JIRA_REPORTER=user@example.com

# Redis configuration
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_PASSWORD=
CACHE_TTL_SECONDS=900

# Optional: enable extra logging during tests
# DEBUG=jml:*
